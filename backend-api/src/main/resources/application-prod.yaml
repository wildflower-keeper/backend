spring:
  datasource:
    url: 'jdbc:mysql://database-1.cluster-cvy6wq0uakvw.ap-northeast-2.rds.amazonaws.com/wildflower_gardening?characterEncoding=UTF-8&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul&useLegacyDatetimeCode=false&zeroDateTimeBehavior=convertToNull&cacheDefaultTimezone=false'
    username: ${RDS_USERNAME}
    password: ${RDS_PW}
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop  # Todo none으로 변경
logging:
  config: classpath:log4j2/log4j2-prod.xml
admin:
  password: ${ADMIN_PASSWORD}
token:
  secret: ${TOKEN_SECRET}